# 桌面收纳

该领域描述用户如何处理堆积在 Windows 桌面上的文件、文件夹与快捷方式，以及如何安全地生成并执行收纳决策。

## Language

**桌面项目**：
实际存在于受监控桌面位置中的文件、文件夹或快捷方式。
_Avoid_: 桌面文件、图标、资源

**主监控目录**：
用户当前选择并由应用监听的桌面或替代目录，是生成可执行整理计划时唯一允许的源根。
_Avoid_: 托管目录、公共桌面、收件箱

**公共桌面只读项目**：
来自 Windows 公共桌面的展示项目，可参与收件箱查看和规则影响预览，但不进入整理计划，也不能由应用移动、保留或忽略。
_Avoid_: 共享收藏夹、主监控目录、只读文件

**收件箱**：
尚未被用户明确处理的桌面项目集合。
_Avoid_: 首页、未分类区、垃圾箱

**收藏夹**：
不改变桌面项目物理位置的逻辑分组，一个桌面项目可以属于多个收藏夹。
_Avoid_: 文件夹、分类目录、分区

**收藏夹成员**：
桌面项目与收藏夹之间按规范化完整路径建立的成员关系；项目暂时不存在时关系仍保留并标记失效，用户可显式移除。
_Avoid_: 文件副本、快捷方式、归档项

**失效成员**：
路径当前不存在的收藏夹成员；失效仅描述关系无法解析，不证明项目已被删除，也不会触发自动清理。
_Avoid_: 已删除文件、无效收藏夹、垃圾项

**成员重新绑定**：
用户在单个收藏夹内把一条失效成员关系替换为另一个现存桌面项目路径；不移动或重命名项目，也不修改其他收藏夹中的关系。
_Avoid_: 文件修复、路径迁移、全局替换

**规则**：
用于判断桌面项目是否匹配，并为匹配项目给出建议动作的条件集合。
_Avoid_: 脚本、筛选器、自动化

**规则条件**：
规则中对扩展名、文件名、大小、修改时间或项目类型的单项约束；同一规则内不同种类的条件必须全部满足，同一种类中的多个候选值满足任意一个即可。
_Avoid_: 规则、筛选项、建议动作

**规则冲突**：
一个桌面项目命中的最高优先级规则中，有两条或更多规则给出不同建议动作或不同归档目标；冲突解决前不能执行整个整理计划。同优先级且建议结果相同不构成冲突。
_Avoid_: 文件重名、执行失败、规则重复

**冲突裁决**：
用户在当前整理计划中为一个规则冲突选择某条已有建议的决定；裁决只对该计划有效，不修改或新增规则，重新生成计划后不会自动沿用。
_Avoid_: 修改规则、默认规则、记住选择

**停用规则**：
仍保留在规则集合中、但不参与整理计划计算的规则，重新启用后恢复原有条件和优先级。
_Avoid_: 删除规则、忽略项

**建议动作**：
规则针对某个桌面项目提出的归档建议，尚未进入文件移动执行器；逻辑分组属于收藏夹关系，保留或忽略属于用户显式设置的项目处置偏好。
_Avoid_: 操作、任务

**整理计划**：
根据当前桌面状态和规则生成、供用户检查的只读变更清单，本身不能修改文件。
_Avoid_: 整理任务、预览结果

**计划排除**：
用户从当前草稿整理计划中移除一个或多个可执行项的临时决定；不修改规则或项目处置偏好，重新生成计划后不沿用。
_Avoid_: 忽略项、删除文件、规则裁决

**计划目标调整**：
用户在当前草稿整理计划中为单个可执行项改选托管目录内的归档子目录；保留项目名称，不修改命中规则，重新生成计划后不沿用。
_Avoid_: 修改规则、重命名项目、选择任意绝对路径

**计划摘要**：
从当前整理计划和对应桌面快照派生的只读统计，包括可执行项、计划排除、规则冲突、已知总大小和目标目录分布；文件夹不递归扫描，其大小明确标记为未知。
_Avoid_: 操作历史、磁盘占用、扫描结果

**执行风险**：
整理计划中会阻止执行或需要在执行前呈现的事实，例如规则冲突、源项目变化、未知文件夹大小和目标重名可能；风险不是已经发生的执行失败。
_Avoid_: 执行结果、错误日志、规则冲突

**执行预检**：
在整理计划执行前，对涉及的源项目、路径边界、占用状态、重解析点、目标介质和已知空间需求进行的不修改文件检查；预检结果分为阻断项与警告项，阻断项存在时不能执行。
_Avoid_: 试运行、整理计划、执行结果

**预检阻断项**：
执行前已经确认会破坏安全边界或无法可靠继续的问题，例如源项目缺失、越过授权目录、重解析点或已知磁盘空间不足。
_Avoid_: 执行失败、风险提示、规则冲突

**预检警告项**：
不阻止执行但需要用户知情的不确定性或安全降级，例如目标已存在将安全改名、文件夹大小未知或目标目录将在执行时创建。
_Avoid_: 忽略、错误、执行授权

**整理操作**：
计划校验与执行预检通过后直接运行的一批文件变更及其完整结果。
_Avoid_: 整理计划、自动整理

**撤销操作**：
把一个或多个已归档项目恢复到桌面范围的独立操作记录；它引用原整理操作但不改写原记录，可只恢复尚未被成功撤销的单个项目。
_Avoid_: 修改历史、回滚事务、恢复计划

**撤销冲突处置**：
撤销时原位置已被占用后，用户对该恢复项显式选择安全改名、跳过或托管范围内的新恢复位置；任何选择都不得覆盖现存项目。
_Avoid_: 目标重名规则、自动覆盖、计划目标调整

**操作项**：
整理操作中针对单个桌面项目的一次变更，包含原位置、目标位置与执行结果。
_Avoid_: 日志、步骤

**托管目录**：
用户指定给软件用于存放已归档桌面项目的根目录。
_Avoid_: 桌面备份、数据库目录

**收纳区**：
托管目录内用于实际存放一类项目的相对子目录；一个收纳区可以被多条规则共同引用。
_Avoid_: 规则、收藏夹、桌面窗口

**收纳窗口**：
收纳区在 Windows 桌面上的可交互映射，展示该收纳区当前存储的项目并允许用户直接管理它们。
_Avoid_: 整理计划、文件夹快捷方式、收藏夹窗口

**桌面组件**：
固定在 Windows 桌面宿主层、用于持续展示或快速操作一类信息的轻量窗口；收纳窗口、快速应用和日历窗口是三种桌面组件。
_Avoid_: 主应用窗口、置顶工具窗口、桌面图标

**快速应用**：
由用户命名的一组快捷目标在桌面上的可交互集合，可打开单个、选中的多个或全部目标。
_Avoid_: 快捷窗口、收纳窗口、Windows 快捷方式、收藏夹

**快捷目标**：
快速应用中一个可启动的网址、本地文件、本地文件夹或程序路径；它只保存引用，不复制或移动目标内容。
_Avoid_: 收纳窗口项目、桌面项目、文件副本

**快速应用分组**：
快速应用内用于组织快捷目标的显示分区；分组只改变窗口中的归类和展示，不改变、复制或移动快捷目标所引用的内容，未指定分组的目标统一显示在“未分组”。
_Avoid_: 文件夹、收纳区、快捷目标路径

**日历窗口**：
固定在桌面宿主层的月历组件，显示日期以及已内置年度中的法定节假日和调休上班标记。
_Avoid_: 日程、提醒、在线日历

**收纳窗口项目**：
当前直接存放在收纳区中的文件、文件夹或快捷方式；它是否由本应用归档不影响其成员身份。
_Avoid_: 桌面项目、收藏夹成员、操作项

**直接收纳**：
用户把文件系统项目拖入收纳窗口后立即移动到对应收纳区的明确操作，不生成整理计划。
_Avoid_: 自动整理、整理计划、复制

**窗口文件操作**：
用户在收纳窗口内发起的打开、复制、粘贴、重命名、移出或移入回收站操作，直接作用于对应的收纳窗口项目；复制粘贴使用 Windows 文件剪贴板，同名目标安全改名而不覆盖。
_Avoid_: 整理操作、计划目标调整、永久删除

**保留项**：
用户明确选择继续放在桌面、暂不再提示整理的桌面项目。
_Avoid_: 忽略项、固定项

**忽略项**：
完全不参与扫描结果、规则匹配和整理建议的桌面项目。
_Avoid_: 保留项、隐藏项

**项目处置偏好**：
用户把某个桌面项目设为保留或忽略的可恢复选择；同一项目只能有一种处置偏好，恢复到收件箱表示不再保留该偏好。
_Avoid_: 规则、建议动作、删除

**备份包**：
规则、通知设置、项目处置偏好与分范围操作历史的只读便携快照；不包含开机启动状态。
_Avoid_: 系统镜像、桌面副本

**恢复计划**：
导入备份包后，经格式版本、包内路径和本机目录边界校验形成的待确认变更摘要；确认前不修改任何设置或历史记录。
_Avoid_: 直接覆盖、自动恢复、整理计划

**诊断事件**：
应用为排查启动、系统集成和运行故障而记录的脱敏技术事件；不参与撤销，不保存文件内容或完整个人目录前缀。
_Avoid_: 整理操作、操作历史、使用分析

**诊断包**：
由用户主动导出的运行环境摘要、最近诊断事件和隐私说明 ZIP；不包含设置文件、操作数据库或桌面项目内容。
_Avoid_: 备份包、崩溃转储、用户数据包
