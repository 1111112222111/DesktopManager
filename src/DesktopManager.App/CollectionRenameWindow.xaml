<Window x:Class="DesktopManager.App.CollectionRenameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="重命名" Width="430" Height="170" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" Background="{StaticResource PorcelainBrush}" ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock FontSize="17" FontWeight="SemiBold" Text="输入新名称" />
        <TextBox x:Name="NameText" Grid.Row="1" Margin="0,12,0,0" Height="36" Padding="9,6"
                 AutomationProperties.Name="收纳窗口项目新名称" />
        <StackPanel Grid.Row="2" Margin="0,16,0,0" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="82" IsCancel="True" Content="取消" />
            <Button Width="92" Margin="10,0,0,0" IsDefault="True" Click="Confirm_Click" Content="确定" />
        </StackPanel>
    </Grid>
</Window>
