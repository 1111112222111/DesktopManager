<Window x:Class="DesktopManager.App.ConflictResolutionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="处理规则冲突" Width="720" Height="430"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource PorcelainBrush}">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock FontSize="23" FontWeight="Bold" Foreground="#18201D" Text="为当前计划选择一个建议" />
        <TextBlock x:Name="SourcePathText" Grid.Row="1" Margin="0,7,0,14" Foreground="{StaticResource GraphiteBrush}"
                   TextWrapping="Wrap" />
        <ListView x:Name="ChoicesList" Grid.Row="2" SelectionChanged="ChoicesList_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="规则" Width="190" DisplayMemberBinding="{Binding RuleName}" />
                    <GridViewColumn Header="建议目标" Width="450" DisplayMemberBinding="{Binding TargetPath}" />
                </GridView>
            </ListView.View>
        </ListView>
        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Foreground="#8A5A00"
                       Text="本次选择只对当前计划有效，不会修改规则。" />
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Padding="14,8" Click="Cancel_Click" Content="取消" />
                <Button x:Name="ConfirmButton" Margin="8,0,0,0" Padding="14,8"
                        IsEnabled="False" Click="Confirm_Click" Style="{StaticResource QuietPrimaryButton}"
                        Content="应用选择" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
